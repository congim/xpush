syntax = "proto3";  //语法声明

package peer; //包名

service Peer {
    rpc OnMessage (Message) returns (Reply) {}
}

message Message {
    string name = 1;
}

// Reply 响应数据格式
message Reply {
    string message = 1;
}

//protoc  --go_out=plugins=grpc:./peer  *.proto
